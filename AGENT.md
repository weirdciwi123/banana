# Agent Guide

## 프로젝트

이 저장소는 성찰 플래너의 기획, 제품 요구사항, 기술 아키텍처, 기술 요구사항을 관리한다. 현재 단계는 구현 전 문서 확정 단계다.

## 기준 문서

작업 전 다음 문서를 확인하고, 문서 간 내용이 충돌하면 PRD와 TRD를 우선 기준으로 삼는다.

1. `PRD.md`: 제품 범위와 수용 기준
2. `IDEATION.md`: 문제 정의, 사용자 여정, 정책, 리스크
3. `ARCHITECTURE.md`: 시스템 구성과 기술 아키텍처
4. `TRD.md`: 구현·테스트·배포 기술 요구사항
5. `README.md`: 프로젝트 개요와 온보딩 안내

`AGENT.md`는 에이전트 작업 규칙을 정의하며 제품 요구사항의 대체 문서가 아니다.

문서의 파일명과 용어는 기존 표기를 유지한다. 특히 `README.md`, `ARCHITECTURE.md`, `Microsoft Agent Framework`, `Copilot SDK` 표기를 사용한다.

## 현재 범위

MVP의 핵심 흐름은 다음과 같다.

`목표 입력 -> 일차 계획 생성 -> 일기 작성 -> 중립형 피드백 -> 사용자 승인형 리플래닝`

포함 기능:

- 웹앱 사용자 인터페이스
- 목표·현재 상태·기간·제약·측정지표 입력
- 일차 계획 생성
- 날짜별 일기 작성 및 조회
- 사실 요약 중심 피드백
- 수용/거절 기반 리플래닝
- 계정 없는 익명 세션 모드
- Docker 이미지 빌드 및 Azure 배포

MVP에서 구현하지 않는 기능:

- 소셜 랭킹
- 강제 스케줄링
- 처벌형 코칭
- 의료 또는 정신건강 진단
- 사용자 계정, 로그인, 회원가입 기능

사용자 계정, 로그인, 회원가입, 계정 연결은 이 앱의 범위에 포함하지 않는다.

## 기술 기준

권장 스택은 TypeScript 단일 언어 기반이다.

- 프론트엔드: Next.js(React) + TypeScript
- API/서버: Node.js + Fastify 또는 NestJS + TypeScript
- 에이전트 오케스트레이션: Microsoft Agent Framework
- AI 연동: Copilot SDK
- 데이터 저장소: Azure Cosmos DB 또는 Azure Database for PostgreSQL
- 컨테이너 레지스트리: Azure Container Registry
- 런타임: Azure Container Apps 우선, Azure App Service for Containers 대안
- 관측성: Application Insights 및 Log Analytics

모델 엔드포인트와 배포 런타임처럼 문서에서 선택지로 남겨 둔 항목은 임의로 확정하지 않는다. 게스트 MVP 저장 방식은 익명 서버 세션으로 확정했으며, 구현을 시작할 때 결정 기록과 관련 문서를 함께 갱신한다.

## 게스트 모드 보안

- 익명 세션은 서버가 발급한 `HttpOnly`·`Secure` 쿠키로 식별한다.
- 클라이언트가 보낸 세션 ID만으로 데이터 소유권을 인정하지 않는다.
- 핵심 데이터에는 게스트 세션 소유 경계를 추적할 수 있는 `guestSessionId`를 포함한다.
- 게스트 데이터의 저장 위치, 브라우저·기기 변경에 따른 유실 가능성, 전체 삭제 방법을 사용자에게 고지한다.
- 로그에는 목표·일기·피드백 원문과 같은 개인정보를 남기지 않거나 마스킹한다.
- AI 키와 데이터베이스 연결 정보는 이미지에 포함하지 않고 환경 변수 또는 Key Vault로 주입한다.
- 상태 변경 요청은 SameSite 쿠키와 Origin 검증 또는 CSRF 토큰으로 보호한다.
- 게스트 세션과 AI 호출에는 요청 빈도와 입력 크기 제한을 적용한다.
- 게스트 데이터는 사용자 삭제 또는 정해진 보관 기간 도달 시 자동 삭제한다.
- 다른 세션의 리소스 요청에는 `404`를 반환한다.
- 세션 쿠키는 운영 환경에서 `HttpOnly`, `Secure`, `SameSite=Lax` 이상으로 설정한다.

## AI 정책

- 피드백은 중립형·사실 요약 중심으로 작성한다.
- 비난, 낙인, 처벌형 문구를 생성하지 않는다.
- 의료 진단, 정신건강 진단, 전문가를 대체하는 조언을 제공하지 않는다.
- 리플래닝은 자동 반영하지 않고 사용자의 `accept` 선택 이후에만 반영한다.
- `reject` 선택 시 기존 계획을 유지하고 결정 이력을 저장한다.
- 모델 출력은 고정 스키마로 정규화하고 정책 엔진 검증을 통과한 경우에만 저장한다.

## 개발 및 변경 규칙

1. 먼저 변경과 직접 관련된 기준 문서와 주변 섹션을 읽는다.
2. 기존 구조와 용어를 유지하며 가장 작은 변경을 적용한다.
3. 제품 범위, API, 데이터 모델, 배포 방식이 바뀌면 관련 문서를 함께 갱신한다.
4. 문서만 있는 현재 단계에서는 실행 가능한 코드를 있다고 가정하지 않는다.
5. 비밀값, 실제 API 키, 개인정보를 파일에 기록하지 않는다.
6. 계정 기능이나 범위 밖 기능을 MVP에 추가하지 않는다.
7. 새 의사결정은 `TRD.md`의 오픈 이슈와 완료 기준에 반영한다.

## 검증 규칙

변경 후 가능한 범위에서 다음을 수행한다.

- `git diff --check`
- 이전 파일명·오탈자·구식 용어 검색
- 문서 간 핵심 용어와 기술요건 교차검증
- API와 데이터 모델의 필드·소유권 정합성 확인
- PRD와 TRD의 API 오류 규약 및 섹션 번호 확인
- 코드가 추가된 이후에는 포맷터, 린터, 타입 검사, 단위·통합·E2E 테스트 실행

문서 단계에서 코드 테스트를 실행할 수 없으면 그 사실을 결과에 명시한다. 검증되지 않은 Azure 배포나 모델 연동을 완료된 것으로 표현하지 않는다.

## 배포 기준

배포 흐름은 다음 순서를 따른다.

1. Docker 이미지 빌드
2. Azure Container Registry에 이미지 푸시
3. Azure Container Apps 또는 Azure App Service for Containers에 배포
4. 환경 변수와 시크릿 주입
5. HTTPS, 헬스체크, 로그, 롤백 경로 확인

배포 전에는 Azure 리소스, 권한, 비용, 시크릿 구성을 확인한다. 실제 배포를 수행하지 않았다면 문서에 배포 완료라고 기록하지 않는다.
