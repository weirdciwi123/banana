{
  "name": "reflection-planner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/src/server.js",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e:deploy": "bash tests/e2e.deploy.smoke.sh",
    "test:all": "npm run test:unit && npm run test:e2e:deploy"
  },
  "dependencies": {
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^11.0.1",
    "@fastify/static": "^10.1.3",
    "@github/copilot-sdk": "^1.0.11",
    "@microsoft/agent-framework": "^0.0.1-beta",
    "fastify": "^5.4.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
